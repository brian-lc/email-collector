const ContactCollector = require('./contact_collector');

describe('storing the email address', () => {

  // Mock DB interface
  // Initialize ContactCollector with the mock
  test('it writes the email to the database', () =>{
    
  })

});